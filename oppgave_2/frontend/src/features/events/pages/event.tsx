

// Componentet for side for ett event

// Brukes i @/app/events/[id]/page.tsx

// Får ferdig prosessert data innsendt.

// F.eks:

/*

export default function EventPage({ event }: { event?: Event }) {
	return (
		<section>
			<h1>{event?.title}</h1>
			<p>{event?.id}</p>
			<p>{JSON.stringify(event?.description ?? " ")}</p>
		</section>
	);
}

*/