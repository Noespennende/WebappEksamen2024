const port = 3999;
export const backendUrl = `http://localhost:${port}`
export const frontendUrl = "http://localhost:4000/"
export const eventUrl = "/events"
export const templateUrl = "/templates"

export { port };
